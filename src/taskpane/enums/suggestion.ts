export enum LevelOfCriticalEnum {
  "CRITICAL" = "critical",
  "HIGH" = "high",
  "MEDIUM" = "middle",
  "LOW" = "low",
}

export enum InsertPlaceEnum {
  "AFTER" = "After",
  "BEFORE" = "Before",
  "REPLACE" = "Replace",
}

export enum ReviewTypesEnums {
  "GENERAL" = "GENERAL",
  "CUSTOM" = "CUSTOM",
}
