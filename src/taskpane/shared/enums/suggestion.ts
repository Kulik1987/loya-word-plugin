export enum LevelOfCriticalEnum {
  "CRITICAL" = "Critical",
  "HIGH" = "High",
  "MEDIUM" = "Medium",
  "LOW" = "Low",
}

export enum InsertPlaceEnum {
  "AFTER" = "After",
  "BEFORE" = "Before",
  "REPLACE" = "Replace",
}
